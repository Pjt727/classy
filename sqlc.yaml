version: "2"
sql:
  - engine: "postgresql"
    schema: "migrations"
    queries: 
      - "data/entry.sql"
      - "data/get.sql"
    gen:
      go:
        package: "db"
        out: "data/db"
        sql_package: "pgx/v5"
